# Simple Agent Job Example - Basic System Check
# Perfect for testing your first agent job

steps:
  - name: Hello World
    action: shell
    command: |
      echo "Hello from Agent: $AGENT_ID"
      echo "Job ID: $JOB_ID"
      echo "Current time: $(date)"
      echo "Working directory: $(pwd)"
    timeout: 30

  - name: System Check
    action: python
    script: |
      import platform
      import os
      
      print(f"🖥️  System: {platform.system()} {platform.release()}")
      print(f"🏷️  Agent: {os.environ.get('AGENT_ID', 'Unknown')}")
      print(f"📋 Job: {os.environ.get('JOB_ID', 'Unknown')}")
      print("✅ Simple job completed successfully!")
    timeout: 60